@model IEnumerable<Secubd.Models.Niveau>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Parcour.libellePar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.libelleNiv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nbreModule)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Parcour.libellePar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.libelleNiv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nbreModule)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codeNiv }) |
            @Html.ActionLink("Details", "Details", new { id=item.codeNiv }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codeNiv })
        </td>
    </tr>
}

</table>
